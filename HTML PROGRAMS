 question3 : 
Imagine you are building a personal portfolio website. Design a web page layout that
includes a header, a navigation menu, a main content area for displaying your projects
and a footer. Explain how you would structure the HTML code and apply CSS styles to
achieve the desired layout and visual design.
ANSWER
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My Portfolio</title>
  <style>
    /* Reset & Base Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      background-color: #f4f4f4;
      color: #333;
    }
QUESTION 11
<!DOCTYPE html>
<html>
<head>
  <title>Days of the Week</title>
</head>
<body>

  <h2>Enter a number (1 to 7) to get the day of the week</h2>

  <form method="post" action="">
    <label for="day_number">Day Number (1-7): </label>
    <input type="number" name="day_number" min="1" max="7" required>
    <input type="submit" value="Show Day">
  </form>

  <br>

  <?php
  if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $day = $_POST['day_number'];

    echo "<h3>Result:</h3>";

    switch ($day) {
      case 1:
        echo "Sunday";
        break;
      case 2:
        echo "Monday";
        break;
      case 3:
        echo "Tuesday";
        break;
      case 4:
        echo "Wednesday";
        break;
      case 5:
        echo "Thursday";
        break;
      case 6:
        echo "Friday";
        break;
      case 7:
        echo "Saturday";
        break;
      default:
        echo "Invalid input. Please enter a number between 1 and 7.";
    }
  }
  ?>

</body>
</html>

QUESTION 12
<!DOCTYPE html>
INPUT:
<html>
<head>
  <title>Student Grade Checker</title>
</head>
<body>

  <h2>Enter Student Marks to Check Grade</h2>

  <form method="post" action="">
    <label for="marks">Enter Marks (%): </label>
    <input type="number" name="marks" min="0" max="100" required>
    <input type="submit" value="Check Grade">
  </form>

  <br>

  <?php
  if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $marks = $_POST['marks'];

    echo "<h3>Result:</h3>";

    if ($marks >= 75 && $marks <= 100) {
      echo "Grade: <strong>First Class</strong>";
    } elseif ($marks >= 60 && $marks < 75) {
      echo "Grade: <strong>Second Class</strong>";
    } elseif ($marks >= 40 && $marks < 60) {
      echo "Grade: <strong>Third Class</strong>";
    } elseif ($marks >= 0 && $marks < 40) {
      echo "Grade: <strong>Fail</strong>";
    } else {
      echo "Invalid marks. Please enter a value between 0 and 100.";
    }
  }
  ?>

</body>
</html>

QUESTION 14
<!DOCTYPE html>
<html>
<head>
  <title>Contact Form</title>
</head>
<body>

<h2>Contact Us</h2>

<form method="post" action="">
  <label for="name">Name:</label><br>
  <input type="text" name="name" required><br><br>

  <label for="email">Email:</label><br>
  <input type="email" name="email" required><br><br>

  <label for="message">Message:</label><br>
  <textarea name="message" rows="5" cols="30" required></textarea><br><br>

  <input type="submit" value="Submit">
</form>

<br>

<?php
// Check if the form is submitted
if ($_SERVER["REQUEST_METHOD"] == "POST") {
  // Collect and sanitize input values
  $name = htmlspecialchars($_POST["name"]);
  $email = htmlspecialchars($_POST["email"]);

  // Display confirmation message
  echo "<h3>Thank you for contacting us, $name!</h3>";
  echo "<p>We have received your message and will get back to you at <strong>$email</strong>.</p>";
}
?>

</body>
</html>

QUESTION 15
<!DOCTYPE html>
<html>
<head>
  <title>Sum of Digits</title>
</head>
<body>

<h2>Enter a Number to Calculate Sum of Digits</h2>

<form method="post" action="">
  <label for="number">Number:</label>
  <input type="number" name="number" required>
  <input type="submit" value="Calculate">
</form>

<br>

<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
  $number = abs((int)$_POST["number"]); // Ensure it's positive integer
  $sum = 0;
  $temp = $number;

  while ($temp > 0) {
    $digit = $temp % 10;  // Extract last digit
    $sum += $digit;       // Add to sum
    $temp = (int)($temp / 10); // Remove last digit
  }

  echo "<h3>Sum of digits of $number is: $sum</h3>";
}
?>

</body>
</html>

QUESTION 16
<!DOCTYPE html>
<html>
<head>
  <title>Sum of Two Numbers</title>
</head>
<body>

<h2>Enter Two Numbers to Find Their Sum</h2>

<form method="post" action="">
  <label>First Number:</label>
  <input type="number" name="num1" required><br><br>

  <label>Second Number:</label>
  <input type="number" name="num2" required><br><br>

  <input type="submit" name="submit" value="Add Numbers">
</form>

<?php
// Function to add two numbers
function addNumbers($a, $b) {
  return $a + $b;
}

// Check if form is submitted
if (isset($_POST['submit'])) {
  $num1 = (int)$_POST['num1'];
  $num2 = (int)$_POST['num2'];
  $sum = addNumbers($num1, $num2);

  echo "<h3>Result: $num1 + $num2 = $sum</h3>";
}
?>

</body>
</html>

QUENTION 17
<!DOCTYPE html>
<html>
<head>
  <title>Sum of Two Numbers</title>
</head>
<body>

<h2>Enter Two Numbers to Find Their Sum</h2>

<form method="post" action="">
  <label>First Number:</label>
  <input type="number" name="num1" required><br><br>

  <label>Second Number:</label>
  <input type="number" name="num2" required><br><br>

  <input type="submit" name="submit" value="Add Numbers">
</form>

<?php
// Function to add two numbers
function addNumbers($a, $b) {
  return $a + $b;
}

// Check if form is submitted
if (isset($_POST['submit'])) {
  $num1 = (int)$_POST['num1'];
  $num2 = (int)$_POST['num2'];
  $sum = addNumbers($num1, $num2);

  echo "<h3>Result: $num1 + $num2 = $sum</h3>";
}
?>

</body>
</html>
<!DOCTYPE html>
<html>
<head>
  <title>Sum of Two Numbers</title>
</head>
<body>

<h2>Enter Two Numbers to Find Their Sum</h2>

<form method="post" action="">
  <label>First Number:</label>
  <input type="number" name="num1" required><br><br>

  <label>Second Number:</label>
  <input type="number" name="num2" required><br><br>

  <input type="submit" name="submit" value="Add Numbers">
</form>

<?php
// Function to add two numbers
function addNumbers($a, $b) {
  return $a + $b;
}

// Check if form is submitted
if (isset($_POST['submit'])) {
  $num1 = (int)$_POST['num1'];
  $num2 = (int)$_POST['num2'];
  $sum = addNumbers($num1, $num2);

  echo "<h3>Result: $num1 + $num2 = $sum</h3>";
}
?>

</body>
</html>

    /* Header */
    header {
      background-color: #333;
      color: #fff;
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
    }

    header h1 {
      font-size: 1.8rem;
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 20px;
    }

    nav a {
      color: white;
      text-decoration: none;
      font-weight: bold;
    }

    nav a:hover {
      text-decoration: underline;
    }

    /* Main Content */
    main {
      padding: 40px;
      background: white;
    }

    #projects h2 {
      margin-bottom: 20px;
    }

    .project-list {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
    }

    .project {
      background: #eaeaea;
      padding: 20px;
      border-radius: 8px;
      transition: transform 0.3s ease;
    }

    .project:hover {
      transform: translateY(-5px);
    }

    /* Footer */
    footer {
      background-color: #333;
      color: #fff;
      text-align: center;
      padding: 20px;
      margin-top: 40px;
    }

    @media (max-width: 600px) {
      header {
        flex-direction: column;
        align-items: flex-start;
      }

      nav ul {
        flex-direction: column;
        gap: 10px;
        margin-top: 10px;
      }
    }
  </style>
</head>
<body>

  <header>
    <h1>John Doe</h1>
    <nav>
      <ul>
        <li><a href="#home">Home</a></li>
        <li><a href="#projects">Projects</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>
  </header>

  <main>
    <section id="projects">
      <h2>My Projects</h2>
      <div class="project-list">
        <div class="project">
          <h3>Project One</h3>
          <p>Description of the first project. Maybe a portfolio website or a blog.</p>
        </div>
        <div class="project">
          <h3>Project Two</h3>
          <p>Description of the second project. Could be an e-commerce site or app.</p>
        </div>
        <div class="project">
          <h3>Project Three</h3>
          <p>Another project, perhaps a mobile app or a design system.</p>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <p>&copy; 2025 John Doe. All rights reserved.</p>
  </footer>

</body>
</html>

question 5
input:
<!DOCTYPE html>
<html>
<head>
    <title>Purchase Cost Calculator</title>
</head>
<body>

<h2>Purchase Cost Calculator</h2>

<form method="post">
    <h3>Item 1:</h3>
    Price: <input type="number" name="price1" step="0.01" required> 
    Quantity: <input type="number" name="quantity1" required><br><br>

    <h3>Item 2:</h3>
    Price: <input type="number" name="price2" step="0.01" required> 
    Quantity: <input type="number" name="quantity2" required><br><br>

    <h3>Item 3:</h3>
    Price: <input type="number" name="price3" step="0.01" required> 
    Quantity: <input type="number" name="quantity3" required><br><br>

    <input type="submit" name="calculate" value="Calculate Total">
</form>

<?php
if ($_SERVER["REQUEST_METHOD"] == "POST" && isset($_POST['calculate'])) {
    // Get values from the form
    $price1 = $_POST['price1'];
    $quantity1 = $_POST['quantity1'];
    $price2 = $_POST['price2'];
    $quantity2 = $_POST['quantity2'];
    $price3 = $_POST['price3'];
    $quantity3 = $_POST['quantity3'];

    // Calculate subtotal
    $subtotal = ($price1 * $quantity1) + ($price2 * $quantity2) + ($price3 * $quantity3);
    $tax = $subtotal * 0.10;
    $total = $subtotal + $tax;

    // Display results
    echo "<h3>Results:</h3>";
    echo "Subtotal: $" . number_format($subtotal, 2) . "<br>";
    echo "Tax (10%): $" . number_format($tax, 2) . "<br>";
    echo "Total Cost: $" . number_format($total, 2) . "<br>";
}
?>

</body>
</html>
output:
Purchase Cost Calculator
Item 1:
Price: 
 Quantity: 


Item 2:
Price: 
 Quantity: 


Item 3:
Price: 
 Quantity: 


Results:"; echo "Subtotal: $" . number_format($subtotal, 2) . "
"; echo "Tax (10%): $" . number_format($tax, 2) . "
"; echo "Total Cost: $" . number_format($total, 2) . "
"; } ?>
question9
input:
 <!DOCTYPE html>
<html>
<head>
    <title>PHP Variable Output Prediction</title>
</head>
<body>

<?php
// DO NOT use $this outside of a class; renamed to $this_var
$this_var = "this";
$that = "that";
$the_other = 2.2000000000;

// $not_set is NOT defined, will be an empty string when used in double-quoted strings
print("$this_var,$not_set,$that+$the_other");
?>

</body>
</html>
output:
 <!DOCTYPE html>
<html>
<head>
    <title>PHP Variable Output Prediction</title>
</head>
<body>


Warning: Undefined variable $not_set in /tmp/BZE1gqgzFj/main.php on line 15
this,,that+2.2
</body>
</html>


=== Code Execution Successful ===
question13
input:
<!DOCTYPE html>
<html>
<head>
    <title>substr() Example</title>
</head>
<body>

<?php
// Convert the number to a string (good practice)
$sub = substr("12345", 2, 2);  // Starts at index 2, extracts 2 characters
echo "sub is $sub";  // Expected output: sub is 34
?>

</body>
</html>
output:
<!DOCTYPE html>
<html>
<head>
    <title>substr() Example</title>
</head>
<body>

<?php
// Convert the number to a string (good practice)
$sub = substr("12345", 2, 2);  // Starts at index 2, extracts 2 characters
echo "sub is $sub";  // Expected output: sub is 34
?>

</body>
</html>
